# Tutte le formule nei JSON

## esercizio_s10_Funzione_integrale_testo_e_svolgimento.json

**Esercizio 0 (formule_usate):**
```latex
F(x) = ∫[a, x] f(t) dt
F'(x) = f(x)
f''(x) = F'''(x)
```

## esercizio_s10_Integrale_improprio_da_appello_settembre_2021.json

**Esercizio 0 (formule_usate):**
```latex
f(x) = \frac{x^2 + \sin(x)}{x^3}
g(x) = \frac{1}{x}
\lim_{x\to \infty} \frac{f(x)}{g(x)} = 1
\int_0^{\infty} \frac{1}{x} dx
```

## esercizio_s10_Itinere3-23-24-testo-e-svolgimento.json

**Esercizio 0 (formule_usate):**
```latex
|z| = √(x² + y²)
```

**Esercizio 1 (formule_usate):**
```latex
|x| = x se x ≥ 0; |x| = -x se x < 0
```

**Esercizio 2 (formule_usate):**
```latex
∫(px/x²) dx = ∫p/x dx
```

**Esercizio 3 (formule_usate):**
```latex
Derivazione di potenze
```

**Esercizio 4 (formule_usate):**
```latex
∫f(x)dx = F(x)
```

## esercizio_s10_integrale_improprio_da_appello_gen_2024_tris.json

**Esercizio 0 (formule_usate):**
```latex
∫ f(x) dx
```

## esercizio_s11_EDO,_complessi,_integrali_impropri_18_dic_2025.json

**Esercizio 0 (formule_usate):**
```latex
y'' + 5y = 0
y_h(x) = c₁ cos(√5 x) + c₂ sin(√5 x)
y_p(x) = Ae^(αx)
```

## esercizio_s1_Itinere1-2023-24.json

**Esercizio 0 (formule_usate):**
```latex
y = (8 ± √(b^2 - 4ac))/(2a)
e^x
```

**Esercizio 1 (formule_usate):**
```latex
|x| = x se x >= 0
|x| = -x se x < 0
```

**Esercizio 2 (formule_usate):**
```latex
(x-a)(x-b)=0
```

**Esercizio 3 (formule_usate):**
```latex
lim (n→∞) 1/n = 0
```

## esercizio_s1_Itinere1_2022-23.json

**Esercizio 0 (formule_usate):**
```latex
e^x > 0
ln(a*b) = ln(a) + ln(b)
y^2 - 8y + 12 = 0
```

**Esercizio 1 (formule_usate):**
```latex
|x| = x se x >= 0
|x| = -x se x < 0
```

**Esercizio 2 (formule_usate):**
```latex
Disequazione di secondo grado
```

**Esercizio 3 (formule_usate):**
```latex
lim (1/n) = 0 quando n -> ∞
```

**Esercizio 4 (formule_usate):**
```latex
Definizioni formali di estremo superiore, massimo, estremo inferiore e minimo
```

## esercizio_s1_Itinere_1_2021-22.json

**Esercizio 0 (formule_usate):**
```latex
sin(a-b) = sin(a)cos(b)-cos(a)sin(b)
cos(2x) = cos²(x) - sin²(x)
cos²(x)+sin²(x)=1
```

**Esercizio 1 (formule_usate):**
```latex
|a| <= b  =>  -b <= a <= b
```

**Esercizio 2 (formule_usate):**
```latex
a/b < c  => a < bc
```

**Esercizio 3 (formule_usate):**
```latex
log(a^b) = b*log(a)
e^(log(x)) = x
```

**Esercizio 4 (formule_usate):**
```latex
|a| = a se a >= 0
|a| = -a se a < 0
```

## esercizio_s2_Es_2_appello_gennaio_2020.json

**Esercizio 0 (formule_usate):**
```latex
lim n→∞ 2/n = 0
e^{-|x|}
```

## esercizio_s2_Esercizi_su_funzioni.json

**Esercizio 0 (formule_usate):**
```latex
log(a) > b  => a > e^b
```

**Esercizio 1 (formule_usate):**
```latex
Formula quadratica: x = (-b ± √(b^2 - 4ac)) / (2a)
```

**Esercizio 2 (formule_usate):**
```latex
log(a) > 0 => a > 1
```

**Esercizio 3 (formule_usate):**
```latex
|x| = x se x >= 0; |x| = -x se x < 0
```

**Esercizio 4 (formule_usate):**
```latex
|x| = x se x >= 0; |x| = -x se x < 0
```

**Esercizio 5 (formule_usate):**
```latex
f^-1(f(x)) = x
```

## esercizio_s2_Itinere1-2024-25.json

**Esercizio 1 (formule_usate):**
```latex
log_a(x) > 0 => x > 1
```

**Esercizio 3 (formule_usate):**
```latex
a^2 - b^2 = (a+b)(a-b)
```

## esercizio_s3_dimostrazione_di_sup.json

**Esercizio 0 (formule_usate):**
```latex
\lim_{n \to \infty} \frac{n}{n+1} = 1
a = \frac{n}{n+1}
n > N
\frac{n}{n+1} > 1 - \epsilon
```

## esercizio_s3_mini-esercitazione-quantificatori.json

**Esercizio 0 (formule_usate):**
```latex
lim x→∞ e^(-x) = 0
```

## esercizio_s4_Teoremi_di_confronto.json

**Esercizio 0 (formule_usate):**
```latex
-1 ≤ sin x ≤ 1
```

**Esercizio 1 (formule_usate):**
```latex
lim y→0 sin(y)/y = 1
sin(y) = y * (sin(y)/y)
```

**Esercizio 2 (formule_usate):**
```latex
0 ≤ cos^2(x) ≤ 1
```

## esercizio_s5_22-23-Itinere2.json

**Esercizio 1 (formule_usate):**
```latex
f'(x) = d/dx [exp(g(x))] = g'(x) * exp(g(x))
```

**Esercizio 2 (formule_usate):**
```latex
lim(sin(x)/x) = 1
```

**Esercizio 3 (formule_usate):**
```latex
log(1+t) ≈ t per t piccolo
```

**Esercizio 4 (formule_usate):**
```latex
z^3 - a = 0 ha soluzioni complesse
```

## esercizio_s5_Esercizio_non_esisitenza_limite.json

**Esercizio 0 (formule_usate):**
```latex
lim_(x→∞) f(x)
sin(nπ) = 0
```

## esercizio_s6_Appello_con_successione.json

**Esercizio 0 (formule_usate):**
```latex
arctan(x) - arctan(y) = arctan((x-y)/(1+xy)
lim_(x->∞) arctan(x) = π/2
```

## esercizio_s6_Appello_gennaio_2023.json

## esercizio_s6_Itinere2-24-25.json

**Esercizio 0 (formule_usate):**
```latex
sin(u) ~ u per u piccolo
eˣ = 1 + x + O(x²)
lim (f(x)/ω(x)) = c
```

**Esercizio 1 (formule_usate):**
```latex
lim (f(g(x))/h(x)) = lim f(g(x)) / lim h(x)
lim c = c
```

**Esercizio 2 (formule_usate):**
```latex
Mantissa(sin(x)) = sin(x)
lim (sin(x)) = 0
```

**Esercizio 3 (formule_usate):**
```latex
lim sin(x) = 0
```

**Esercizio 4 (formule_usate):**
```latex
Continuità di una funzione
```

## esercizio_s7_Esercitazione_con_Landau.json

**Esercizio 0 (formule_usate):**
```latex
lim_(x->+∞) (x + sin(2x)) / (x^2 + (3√x^2 + e^(-x))) = 0
```

**Esercizio 1 (formule_usate):**
```latex
tan(√x) ~ √x per x->0+
log(1+2x) ~ 2x per x->0+
cos(2x) ~ 1 - 2x^2 per x->0+
```

**Esercizio 2 (formule_usate):**
```latex
lim_(n->+∞) (2n + (-1)^n sin(n)) / (n^2 + 1) = ∞
```

**Esercizio 3 (formule_usate):**
```latex
cos(x^3) ~ 1 - x^6/2 per x->0
cos(3x) ~ 1 - (9x^2)/2 per x->0
log(1-y) ~ -y per y vicino a zero
```

**Esercizio 4 (formule_usate):**
```latex
sin(u) ~ u per u->0
cosh(v) ~ 1 per v->0
```

## esercizio_s7_Itinere2-23-24.json

**Esercizio 0 (formule_usate):**
```latex
\lim_{x \to 0} sin(x) = 0
\lim_{x \to 0} e^{-x^2} = 1
sin(x) = x cos(x) for small x
```

**Esercizio 1 (formule_usate):**
```latex
arccos is decreasing
```

**Esercizio 2 (formule_usate):**
```latex
\frac{d}{dx} log_a(u) = \frac{1}{ln(a)} \frac{1}{u} \frac{du}{dx}
Chain rule for derivatives
```

**Esercizio 3 (formule_usate):**
```latex
Limits of exponential functions
```

**Esercizio 4 (formule_usate):**
```latex
Definition of derivative
```

## esercizio_s8_mini-esercitazione-derivabilita.json

**Esercizio 0 (formule_usate):**
```latex
lim (x->a) f(x) = L
f'(x) = lim_{h->0} (f(x+h)-f(x))/h
```

## esercizio_s9_Esercizi_con_Taylor.json

**Esercizio 0 (formule_usate):**
```latex
f(x) = 3 - (x+1)^4 + o((x+1)^4)
f'(x) = -4(x+1)^3
f''(x) = -12(x+1)^2
```

**Esercizio 1 (formule_usate):**
```latex
f(x) = 3 + 10(x+2)^5 - 35(x+2)^7 + o((x+2)^7)
f'(x) = 50(x+2)^4 - 245(x+2)^6
f''(x) = 200(x+2)^3 - 1470(x+2)^5
```

**Esercizio 2 (formule_usate):**
```latex
cos x = 1 - x^2/2 + o(x^2)
(1-x^2)^(1/2) = 1 - x^2/2 + o(x^2)
√(1 - x^2/2 + o(x^2)) = 1 - x^2/4 + o(x^4)
```

## esercizio_s9_Itinere3-24-25.json

**Esercizio 0 (formule_usate):**
```latex
Regola della catena: d/dx [f(g(x))] = f'(g(x)) * g'(x)
```

**Esercizio 1 (formule_usate):**
```latex
Sviluppo di Maclaurin per log(1+x)
Sviluppo di Maclaurin per cos(x)
```

**Esercizio 2 (formule_usate):**
```latex
Definizione di derivata
Proprietà del valore assoluto
```

**Esercizio 3 (formule_usate):**
```latex
Regola della sostituzione per integrali
```

**Esercizio 4 (formule_usate):**
```latex
Definizione di modulo di un numero complesso
```

## lezione_edo.json

**Concetto 0 (Equazione Differenziale Ordinaria (EDO)):**
```latex
y'(x) + f(x)y(x) = g(x)
```

**Concetto 1 (Soluzione di un'EDO):**
```latex
y(x) = g(x) + c
```

**Concetto 2 (Integrale Generale e Particolare):**
```latex
\text{ Integrale Generale }: y(x) = g(x) + c; \text{ Integrale Particolare }: y(x) = g(x) + c_0 (\text{ \text{ dove c_0 è determinato dalle condizioni iniziali }) }
```

**Concetto 3 (Equazione Autonoma):**
```latex
dy/dx = f(y)
```

## lezione_esfin.json

**Concetto 1 (Asintoti):**
```latex
Asintoto verticale: lim x→c f(x) = ±∞; Asintoto orizzontale: lim x→±∞ f(x) = L; Asintoto obliquo: y = mx + q, dove m = lim x→±∞ f(x)/x e q = lim x→±∞ [f(x) - mx].
```

**Concetto 2 (Continuità e Derivabilità):**
```latex
Continuità: lim x→c f(x) = f(c); Derivabilità: f'(x) = lim h→0 [f(x+h)-f(x)]/h
```

**Concetto 3 (Monotonia e Punti Estremi):**
```latex
f'(x) > 0: crescente; f'(x) < 0: decrescente
```

**Concetto 4 (Parte Principale dell'Infinitesimo):**
```latex
f(x) ~ g(x) come x → a, significa lim (x→a) f(x)/g(x) = 1
```

**Concetto 5 (Integrale Improprio):**
```latex
∫[a, ∞] f(x) dx = lim b→∞ ∫[a, b] f(x) dx
```

## lezione_l1.json

**Concetto 0 (Proposizioni Logiche):**
```latex
p, q, r\dots \text{ (simboli che rappresentano le proposizioni)}
```

**Concetto 1 (Predicati Logici):**
```latex
P(x), Q(x, y)\dots \text{ dove } P \text{ e } Q \text{ sono i nomi dei predicati e } x, y \text{ sono le variabili appartenenti al dominio.}
```

**Concetto 2 (Condizioni Sufficienti e Necessarie):**
```latex
p \to q \text{ (p è sufficiente per q)}, \neg q \to \neg p \text{ (p è necessario per q)}. \text{ Equivalentemente, } p \to q \text{ è logicamente equivalente a } \neg q \to \neg p.
```

## lezione_l10.json

**Concetto 0 (Funzione Composta):**
```latex
(f ∘ g)(x) = f(g(x))
```

**Concetto 1 (Teorema del Limite della Funzione Composta):**
```latex
lim_(x→x₀) (f ∘ g)(x) = lim_(y→g(x₀)) f(y)
```

**Concetto 2 (Condizioni di Domini):**
```latex
dom(f) ⊇ \{ g(x) : x ∈ (x₀ - ε, x₀ + ε) \} \text{ e } dom(g) ⊇ (x₀ - ε, x₀ + ε)
```

## lezione_l11.json

**Concetto 0 (Funzione Composta):**
```latex
(f ∘ g)(x) = f(g(x))
```

**Concetto 1 (Teorema della Funzione Composta per i Limiti):**
```latex
lim_(x→c) (f ∘ g)(x) = lim_(y→L) f(y)
```

## lezione_l12.json

**Concetto 0 (Successione):**
```latex
a_n
```

**Concetto 1 (Limite di una Successione):**
```latex
lim_(n→∞) a_n = L
```

**Concetto 2 (Successioni Monotone):**
```latex
a_{n+1} ≥ a_n (crescente), a_{n+1} ≤ a_n (decrescente)
```

**Concetto 3 (Teorema del Confronto):**
```latex
an ≤ bn  e lim_(n→∞) an = L ⇒ lim_(n→∞) bn = L
```

## lezione_l13.json

**Concetto 0 (Successione Geometrica):**
```latex
a_n = a * r^(n-1)
```

**Concetto 1 (Teorema di Esistenza degli Zeri (o Teorema di Bolzano)):**
```latex
\text{ Se  }f(a) * f(b) < 0,\text{  allora  }∃ c ∈ (a, b)\text{  tale che  }f(c) = 0
```

**Concetto 2 (Condizione di Monotonia per l'Unicità dello Zero):**
```latex
\text{ Se  }f \text{ è strettamente monotona su } [a, b],\text{  allora  }\text{ esiste un unico } c ∈ (a, b)\text{  tale che  }f(c) = 0
```

## lezione_l14.json

**Concetto 0 (Continuità):**
```latex
lim_(x→x₀) f(x) = f(x₀)
```

**Concetto 1 (Teorema di esistenza degli zeri):**
```latex
f: [a, b] → ℝ \text{ continua; } f(a) * f(b) ≤ 0 ⇒ ∃ c ∈ (a, b): f(c) = 0
```

**Concetto 2 (Monotonia):**
```latex
x₁ < x₂ ⇒ f(x₁) ≤ f(x₂) (crescente);  x₁ < x₂ ⇒ f(x₁) ≥ f(x₂) (decrescente)
```

## lezione_l15.json

**Concetto 0 (Teorema dei Valori Intermedi (TVI)):**
```latex
Sia f : [a, b] → ℝ \text{ continua; } allora, ∀ y ∈ ℝ ∃ c ∈ [a, b]\text{  tale che  }f(c) = y.
```

**Concetto 1 (Simboli di Landau: O, o, Θ):**
```latex
f(x) = O(g(x)) ⇔ ∃ C > 0, ∀ x ∈ X: 0 ≤ f(x) ≤ C * g(x)
f(x) = o(g(x)) ⇔ lim (x→∞) f(x)/g(x) = 0
f(x) = Θ(g(x)) ⇔ f(x) = O(g(x)) e f(x) = o(g(x))
```

## lezione_l16.json

**Concetto 0 (Infinitesimo):**
```latex
lim_(x→x₀) f(x)/y = 0
```

**Concetto 1 (Parte Principale):**
```latex
p.p.(f(x);y(x)) = lim_(x→x₀) (f(x)/y(x)) - [termine infinitesimo]
```

**Concetto 2 (Asintoto):**
```latex
lim_(x→∞) [f(x) - (mx + q)] = 0
```

## lezione_l17.json

**Concetto 0 (Tasso Incrementale):**
```latex
\frac{\Delta y}{\Delta x}
```

**Concetto 1 (Derivata):**
```latex
f'(x) = \lim_{h 	o 0} rac{f(x+h) - f(x)}{h}
```

## lezione_l18.json

**Concetto 0 (Derivata di una funzione composta):**
```latex
f'(x) = g'(h(x)) * h'(x)
```

**Concetto 2 (Condizioni per la derivabilità di una funzione composta):**
```latex
f derivabile in c \text{ se  }h'(c) ≠ 0 e g derivabile in h(c)
```

## lezione_l19.json

**Concetto 0 (Teorema di Rolle):**
```latex
f(a) = f(b) ⇒ ∃ c ∈ (a, b): f'(c) = 0
```

**Concetto 1 (Teorema del Valor Medio):**
```latex
f'(c) = (f(b) - f(a)) / (b - a)
```

**Concetto 2 (Teorema di Fermat):**
```latex
\text{ Se  }f \text{ ha un massimo/minimo locale in } c ⇒ f'(c) = 0
```

## lezione_l2.json

**Concetto 0 (Quantificatore Universale (∀)):**
```latex
∀x P(x)
```

**Concetto 1 (Quantificatore Esistenziale (∃)):**
```latex
∃x P(x)
```

**Concetto 2 (Negazione di Quantificatori):**
```latex
¬∀x P(x) ≡ ∃x ¬P(x)
```

**Concetto 3 (Insiemi Numerici: N, Z, Q, R):**
```latex
N = {0, 1, 2, ...}, Z = {..., -2, -1, 0, 1, 2, ...}, Q = {p/q | p ∈ Z, q ∈ Z, q ≠ 0}, R = \text{ tutti i numeri che possono essere rappresentati su una retta numerica. }
```

## lezione_l20.json

**Concetto 0 (Formula dell'Incremento Finito):**
```latex
f(x + h) = f(x) + f'(x)h + o(h)
```

**Concetto 1 (Teorema di Lagrange (Forma Generale)):**
```latex
f(b) - f(a) = f'(c)(b - a)
```

**Concetto 2 (Sviluppo di Taylor):**
```latex
f(x) = f(a) + f'(a)(x-a) + rac{f''(a)}{2!}(x-a)^2 + ... + rac{f^{(n)}(a)}{n!}(x-a)^n + R_n(x)
```

## lezione_l21.json

**Concetto 0 (Sviluppo di McLaurin):**
```latex
f(x) = ∑[n=0 to ∞] (f^(n)(0)/n!) * x^n
```

**Concetto 1 (Concavità e Convessità):**
```latex
f'' > 0 (convessa), f'' < 0 (concava)
```

**Concetto 2 (Punto di Flesso):**
```latex
f''(x) = 0 oppure f''(x) non esiste
```

## lezione_l22.json

**Concetto 0 (Convessità/Concavità):**
```latex
f''(x) > 0  => f(x) convessa; f''(x) < 0 => f(x) concava
```

**Concetto 1 (Punti di Flesso):**
```latex
f''(x) = 0 e f'''(x) ≠ 0 (o non esiste)
```

**Concetto 2 (Sviluppo di Taylor (con resto di Peano)):**
```latex
f(x) = f(x₀) + f'(x₀)(x-x₀) + rac{f''(x₀)}{2!}(x-x₀)² + ... + R_n(x)
```

## lezione_l23.json

**Concetto 0 (Funzione Primitiva):**
```latex
F(x) + C
```

**Concetto 1 (Integrale Indefinito):**
```latex
∫f(x) dx = F(x) + C
```

**Concetto 2 (Regole di Integrazione):**
```latex
\text{ Regole specifiche a seconda del tipo di integrale }
```

## lezione_l24.json

**Concetto 0 (Integrale Definito):**
```latex
∫ab f(x) dx = limn→∞ ∑i=1n f(xi) Δxi
```

**Concetto 1 (Somme di Riemann (Inferiore e Superiore)):**
```latex
S_n = ∑i=1n m_i Δx_i,   T_n = ∑i=1n M_i Δx_i
```

**Concetto 2 (Condizione di Integrabilità):**
```latex
lim ||P||→0 |T_n - S_n| = 0
```

## lezione_l25.json

**Concetto 0 (Funzione Primitiva (o Antiderivata)):**
```latex
F'(x) = f(x)
```

**Concetto 1 (Integrale Definito):**
```latex
∫ab f(x) dx
```

**Concetto 2 (Teorema Fondamentale del Calcolo Integrale (TFCI)):**
```latex
∫ab f(x) dx = F(b) - F(a)
```

## lezione_l26.json

**Concetto 0 (Integrale improprio con dominio illimitato):**
```latex
∫ₐ^∞ f(x) dx = lim ₕ→∞ ∫ₐ^ₕ f(x) dx
```

**Concetto 1 (Integrale improprio con singolarità):**
```latex
∫ₐ^b f(x) dx = lim ϵ→0+ ∫ₐ^(c-ϵ) f(x) dx + lim ϵ→0+ ∫_(c+ϵ)^b f(x) dx
```

**Concetto 2 (Criterio del confronto):**
```latex
\text{ Se  }0 ≤ f(x) ≤ g(x), allora:
- ∫ₐ^∞ g(x) dx converges ⇒ ∫ₐ^∞ f(x) dx converges
- ∫ₐ^∞ f(x) dx diverges ⇒ ∫ₐ^∞ g(x) dx diverges
```

## lezione_l3.json

**Concetto 0 (Insieme Limitato Superiormente):**
```latex
∃ M ∈ ℝ : ∀ a ∈ A, a ≤ M
```

**Concetto 1 (Estremo Superiore (sup)):**
```latex
sup(A) = {M ∈ ℝ : M \text{ è un limite superiore di } A} ∩ ℝ
```

**Concetto 2 (Massimo (max)):**
```latex
max(A) = {a ∈ A : ∀ b ∈ A, a ≤ b}
```

**Concetto 3 (Insieme Limitato Inferiormente):**
```latex
∃ m ∈ ℝ : ∀ a ∈ A, a ≥ m
```

**Concetto 4 (Estremo Inferiore (inf)):**
```latex
inf(A) = {m ∈ ℝ : m \text{ è un limite inferiore di } A} ∩ ℝ
```

**Concetto 5 (Minimo (min)):**
```latex
min(A) = {a ∈ A : ∀ b ∈ A, a ≥ b}
```

## lezione_l4.json

**Concetto 0 (Funzione Iniettiva (o Iniezione)):**
```latex
∀x₁, x₂ ∈ A, f(x₁) = f(x₂) ⇒ x₁ = x₂
```

**Concetto 1 (Funzione Suriettiva (o Suringettive)):**
```latex
∀y ∈ B, ∃x ∈ A, f(x) = y
```

**Concetto 2 (Funzione Biettiva):**
```latex
f \text{ è iniettiva } ∧ f \text{ è suriettiva }
```

**Concetto 3 (Funzione Inversa):**
```latex
f⁻¹(f(x)) = x, ∀x ∈ A;  f(f⁻¹(y)) = y, ∀y ∈ B
```

## lezione_l5.json

**Concetto 0 (Funzione Limitata):**
```latex
f(x) ≤ M ∀ x ∈ A  (limitata superiormente)
f(x) ≥ m ∀ x ∈ A  (limitata inferiormente)
```

**Concetto 1 (Massimo e Minimo Assoluto):**
```latex
x₀ \text{ è un punto di max assoluto } \text{ se  }f(x₀) ≥ f(x) ∀ x ∈ A
x₁ \text{ è un punto di min assoluto } \text{ se  }f(x₁) ≤ f(x) ∀ x ∈ A
```

**Concetto 2 (Funzione Monotona):**
```latex
f(x₁) ≤ f(x₂) \text{ se  }x₁ < x₂ (crescente)
f(x₁) < f(x₂) \text{ se  }x₁ < x₂ (strettamente crescente)
f(x₁) ≥ f(x₂) \text{ se  }x₁ < x₂ (decrescente)
f(x₁) > f(x₂) \text{ se  }x₁ < x₂ (strettamente decrescente)
```

## lezione_l6.json

**Concetto 0 (Limite di una funzione):**
```latex
lim_{x 	o c} f(x) = L
```

**Concetto 1 (Continuità):**
```latex
f(c) = lim_{x 	o c} f(x)
```

## lezione_l7.json

**Concetto 1 (Limiti di una funzione):**
```latex
lim_(x→c) f(x) = L
```

**Concetto 2 (Continuità di una funzione):**
```latex
f(c) = lim_(x→c) f(x)
```

## lezione_l8.json

**Concetto 0 (Dominio Illimitato):**
```latex
lim_(x→∞) f(x)
```

**Concetto 1 (Immagine Illimitata):**
```latex
lim_(x→a) f(x) = ±∞
```

**Concetto 2 (Asintoti Verticali):**
```latex
lim_(x→c) f(x) = ±∞
```

**Concetto 3 (Asintoti Orizzontali):**
```latex
lim_(x→±∞) f(x) = L
```

## lezione_l9.json

**Concetto 0 (Teorema di Permanenza del Segno):**
```latex
lim_(x→c) f(x) = L ≠ 0 ⇒ ∃ (a, b) ⊆ (c - ε, c + ε)\text{  tale che  }sgn(f(x)) = sgn(L) \text{ per ogni } x ∈ (a, b)
```

**Concetto 1 (Teoremi sull'Algebra dei Limiti):**
```latex
lim_(x→c) [f(x) ± g(x)] = lim_(x→c) f(x) ± lim_(x→c) g(x)
lim_(x→c) [f(x) * g(x)] = lim_(x→c) f(x) * lim_(x→c) g(x)
lim_(x→c) [f(x) / g(x)] = lim_(x→c) f(x) / lim_(x→c) g(x),  g(c) ≠ 0
```

**Concetto 2 (Teoremi di Confronto (Carabinieri)):**
```latex
f(x) ≤ g(x) ≤ h(x), lim_(x→c) f(x) = L = lim_(x→c) h(x) ⇒ lim_(x→c) g(x) = L
```

## quiz_extra.json

## riassunto.json

## teoremi.json
